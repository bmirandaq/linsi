name = "linsi-form-handler"
main = "index.js"
compatibility_date = "2025-08-31"

[vars]
ALLOWED_ORIGIN = "https://linsi.beamiranda.com.br"

# Configure via: wrangler secret put TURNSTILE_SECRET_KEY
# Configure via: wrangler secret put NOTION_API_KEY
# Configure via: wrangler secret put NOTION_DATABASE_ID
# Configure via: wrangler secret put RESEND_API_KEY
# Configure via: wrangler secret put CONTACT_TO_EMAIL
# Configure via: wrangler secret put CONTACT_FROM_EMAIL
# Configure via: wrangler secret put WORKSHOP_NOTION_DATABASE_ID
# Configure via: wrangler secret put WORKSHOP_COUPONS_JSON
# Configure via: wrangler secret put MP_ACCESS_TOKEN
# Configure via: wrangler secret put MP_PUBLIC_KEY
# Configure via: wrangler secret put MP_WEBHOOK_SECRET
