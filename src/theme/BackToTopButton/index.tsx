import React, {type ReactNode} from 'react';

// The platform-level button is mounted once in src/theme/Root.tsx.
// Keep Docusaurus' native insertion points empty to avoid duplicate buttons in docs.
export default function BackToTopButton(): ReactNode {
  return null;
}
